---
import { getCollection } from "astro:content";
import ContentListLayout from "@/layouts/ContentListLayout.astro";
import ContentCard from "@/components/ContentCard.astro";

const projects = (await getCollection("projects"))
  .sort((a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime());
---

<ContentListLayout
  title="Projects"
  pageTitle="Projects | Siddhant Shah"
  description="Quantitative finance projects, trading strategy implementations, and data analytics work."
  contentType="projects"
>
  {projects.map(project => (
    <ContentCard
      type="projects"
      slug={project.slug}
      title={project.data.title}
      description={project.data.description}
      publishedAt={project.data.publishedAt}
    />
  ))}
</ContentListLayout>
