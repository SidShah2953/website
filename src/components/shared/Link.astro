---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a"> & {
  label: string;
  isUnderline: string;
};

const { label, isUnderline, href } = Astro.props;
---

<a
  href={href}
  class:list={{
    ["hover:text-neutral-100 cursor-pointer"]: true,
    ["underline"]: (isUnderline == 'True'),
  }}
>
  {label}
</a>
