---
import { SEO } from "astro-seo";
import { SITE_URL } from "@/data/config";
import type { HeadTags } from "@/utils/types/HeadTags";

type Props = HeadTags;

const { title, description, noindex, og } = Astro.props;

const DEFAULT_TITLE_PAGE = "Siddhant Shah";
const DEFAULT_DESCRIPTION_PAGE =
  "I'm a final year student, pursuing a B.Sc. in Math and CS from CMI";
const DEFAULT_URL_SITE = SITE_URL;

const openGraph = {
  title: "Hi! I'm Siddhant Shah",
  type: "website",
  image: "/social-prev.png",
  alt: "Siddhant Shah",
  url: DEFAULT_URL_SITE,
  description: DEFAULT_DESCRIPTION_PAGE,
};
---

<head>
  <SEO
    charset="UTF-8"
    title={title || DEFAULT_TITLE_PAGE}
    description={description || DEFAULT_DESCRIPTION_PAGE}
    noindex={noindex || false}
    openGraph={{
      basic: {
        title: openGraph.title,
        type: openGraph.type,
        image: openGraph.image,
        url: openGraph.url,
      },
      optional: {
        description: openGraph.description,
      },
      image: {
        alt: openGraph.alt,
      },
    }}
    extend={{
      link: [
        { rel: "icon", href: "/favicon.svg" },
        { rel: "sitemap", href: "/sitemap-index.xml" },
      ],
      meta: [
        { name: "viewport", content: "width=device-width, initial-scale=1" },
        { name: "generator", content: Astro.generator },
      ],
    }}
  />
</head>
